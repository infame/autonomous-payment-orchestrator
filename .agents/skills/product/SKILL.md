---
name: product
description: Persist product hypotheses for owner scoring; optional theme or constraint.
---

Delegate $ARGUMENTS to product Mode A with a compact handoff. It reads PRODUCT.md, relevant code/signals, available memory, and open issues. Resolve duplicate ideas before presentation.

Before asking for scores, persist every complete card and an empty score/note row in PRODUCT.md under the dated Hypothesis log. Allocate H<n> above the maximum ID anywhere in the document; never renumber or reuse IDs. Check the current document before writing so concurrent edits are preserved. Present the persisted cards and Owner's call table; stop without implementation.

On owner scores, update the existing rows/cards in place. Retain full cards for all scores, including rejected ones; record score 1 and the owner's reason in Rejected with the same ID. Do not invent a reason when none was given. Offer `/spec H<n>` for scores 4–5. Update available memory only when useful; PRODUCT.md remains the durable source and scoring does not require another delegation.
