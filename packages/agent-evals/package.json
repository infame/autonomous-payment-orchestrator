{
  "name": "@apo/agent-evals",
  "version": "0.1.0",
  "private": true,
  "description": "Adversarial eval harness for @apo/agent-orchestrator: scenarios, safety invariants, metrics.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @apo/agent-orchestrator build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "pretest": "pnpm --filter @apo/agent-orchestrator build",
    "test": "vitest run",
    "test:watch": "vitest",
    "prelint": "pnpm --filter @apo/agent-orchestrator build",
    "lint": "eslint src"
  },
  "dependencies": {
    "@apo/agent-orchestrator": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.19.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
